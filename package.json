{
  "name": "fresh_news",
  "version": "1.0.0",
  "description": "FreshNews app",
  "main": "src/app.js",
  "scripts": {
    "start": "http-server dist --silent & watchify -o dist/app.js --verbose --debug . --extension .jsx",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jan Dudek <jd@jandudek.com> (http://github.com/jdudek)",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^8.1.3",
    "http-server": "^0.7.4",
    "watchify": "^2.3.0"
  },
  "dependencies": {
    "jquery": "^2.1.3",
    "react": "^0.12.2",
    "reactify": "^1.0.0",
    "react-bootstrap": "^0.15.1",
    "react-router": "^0.12.0"
  },
  "browserify": {
    "transform": [
      "reactify"
    ]
  }
}
